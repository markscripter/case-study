type StopPanelProps = {
    children?: JSX.Element | JSX.Element[]
}

export const disclaimer = "hello foooter"

export default function StopPanel({ }: StopPanelProps) {
    return (
        <div>
            STOPS PANEL
        </div>
    )
}