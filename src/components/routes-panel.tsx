type RoutesPanelProps = {
    children?: JSX.Element | JSX.Element[]
}

export const disclaimer = "hello foooter"

export default function RoutesPanel({ }: RoutesPanelProps) {
    return (
        <div>
            ROUTES PANEL
        </div>
    )
}